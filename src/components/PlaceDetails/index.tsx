import React, { FC } from "react";

interface IPlaceDetailsProps {}

const PlaceDetails: FC<IPlaceDetailsProps> = (props) => {
  return <React.Fragment>Header</React.Fragment>;
};

export default PlaceDetails;
